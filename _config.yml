theme: minima
repository:
    siruix/siruix.github.io
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"
    values:
      layout: "default"